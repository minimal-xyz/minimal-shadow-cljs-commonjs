{
  "name": "minimal-shadow-cljs",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "del": "rm -f compiled/*",
    "compile": "yarn del && shadow-cljs -b release --once",
    "release": "yarn del && shadow-cljs -b release --release",
    "watch": "yarn del && shadow-cljs -b release --dev"
  },
  "author": "jiyinyiyong",
  "license": "MIT",
  "keywords": [
    "ClojureScript"
  ],
  "description": "",
  "devDependencies": {
    "shadow-cljs": "^0.9.4"
  }
}
