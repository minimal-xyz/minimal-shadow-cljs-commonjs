{
  "name": "minimal-shadow-cljs",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "del": "rm -f compiled/*",
    "compile": "yarn del && shadow-cljs --once",
    "release": "yarn del && shadow-cljs -b release --release",
    "watch": "yarn del && shadow-cljs --dev"
  },
  "author": "jiyinyiyong",
  "license": "MIT",
  "keywords": [
    "ClojureScript"
  ],
  "description": "",
  "shadow-cljs": {
    "source-paths": [
      "src/"
    ],
    "output-dir": "compiled/"
  },
  "devDependencies": {
    "shadow-cljs": "^0.8.0"
  }
}
